components:
  schemas:
    {{pascalCase name}}:
      title: {{pascalCase name}}
      type: object
      required:
        - email
        - password
      properties:
        email:
          type: string
          format: email
        password:
          type: string
          format: password