import request from 'supertest';
import app from '~/index';

describe('[{{upperCase method}}] {{pascalCase rootRoute}} - {{pascalCase routeName}}', () => {
  // TODO: Change Description
  it('should do something here', async () => {
    const res = await request(app)
      .post('/api/{{lowerCase version}}/{{lowerCase rootRoute}}/{{camelCase routeName}}')
      .send({
        // TODO: Change Values
        username: '',
        password: '',
      });
    expect(res.status).toEqual(200);
    // expect(res.body).toHaveProperty('post');
  });
});
