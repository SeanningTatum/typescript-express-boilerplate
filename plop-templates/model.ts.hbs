import mongoose, { Schema, Document } from 'mongoose';

/**
 * @swagger
 *
 * definitions:
 *   {{pascalCase name}}:
 *     type: object
 *     required:
 *       - sample
 *     properties:
 *       id:
 *         type: string
 *         format: id
 *        sample:
 *          type: string
 */

export interface I{{pascalCase name}} extends Document {
    // TODO: Add properties 
    sample: string;
}

const {{pascalCase name}}Schema = new Schema({
  // TODO: Add properties 
  sample: { type: String, required: true },
});

export default mongoose.model<I{{pascalCase name}}>('{{pascalCase name}}', {{pascalCase name}}Schema);
