import { Router } from 'express';
import validate from '~/utils/validate';
/* PLOP_INJECT_IMPORT */

const router = Router();

/**
 * @swagger
 * tags:
 *   name: {{pascalCase name}}
 *   description: {{pascalCase name}} management - CRUD
 */

/* PLOP_INJECT_ROUTE */

export default router;
