import { Router } from 'express';
import validate from '~/utils/validate';
// import create{{pascalCase name}}, { create{{pascalCase name}}Params } from './create{{pascalCase name}}';

const router = Router();

/**
 * @swagger
 * tags:
 *   name: {{pascalCase name}}
 *   description: {{pascalCase name}} management - CRUD
 */

// router.get('/:id', validate(get{{pascalCase name}}ByIdParams), get{{pascalCase name}}ById);
// router.get('/', validate(get{{pascalCase name}}ListParams), get{{pascalCase name}}List);
// router.post('/create{{pascalCase name}}', validate(create{{pascalCase name}}Params), create{{pascalCase name}});
// router.put('/update{{pascalCase name}}', validate(update{{pascalCase name}}Params), update{{pascalCase name}});
// router.delete('/delete{{pascalCase name}}', validate(delete{{pascalCase name}}Params), delete{{pascalCase name}});

export default router;
